hostname "F&T Base Visual Test"
sv_lan 1
sv_password "ft_base_visual_test"
rcon_password "ft_base_visual_test"
sv_hibernate_think 0
log on

// Change both passwords before exposing the server outside the local network.
